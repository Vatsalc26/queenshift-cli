# Queenshift Demo Repo Pack

This repo is disposable and non-production by design.

`demo:run` stages a fresh git workspace from this template, runs one trusted bounded task, and points you at the normal run artifacts plus git diff hints.
